namespace Src.DataClasses
{
    public enum Category : byte
    {
        // these correspond the the ids on the DB move_damage_classes table
        Status = 1,
        Physical = 2,
        Special = 3,
    }
}
